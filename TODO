gamp TODO (as of 1.0.1):

   -color?
   -support for oog vorbis files
   -support for streaming mp3s
   -extended playlist functionality, including selecting multiple files,
    cropping, multi-file add/remove/replace
   -backgrounding/foregrounding
   -any other interesting items i (or you) can think of
